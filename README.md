# Media renamer

Tools for renaming media files to add (year) and [tmdbid-###] tags.

Add your [TMDB API token and key](https://developer.themoviedb.org/docs/getting-started) to `.env.template` and rename to `.env`.
